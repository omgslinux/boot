{% extends 'base.html.twig' %}

{% block title %}BookOld{% endblock %}

{% block body %}
    <h1>Tabla de Reservas</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>TipoDoc</th>
                <td>{{ book_old.docType }}</td>
            </tr>
            <tr>
                <th>DocNumber</th>
                <td>{{ book_old.docNumber }}</td>
            </tr>
            <tr>
                <th>Surname1</th>
                <td>{{ book_old.surname1 }}</td>
            </tr>
            <tr>
                <th>Surname2</th>
                <td>{{ book_old.surname2 }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ book_old.name }}</td>
            </tr>
            <tr>
                <th>Nationality</th>
                <td>{{ book_old.nationality }}</td>
            </tr>
            <tr>
                <th>City</th>
                <td>{{ book_old.city }}</td>
            </tr>
            <tr>
                <th>Home</th>
                <td>{{ book_old.home }}</td>
            </tr>
            <tr>
                <th>Cp</th>
                <td>{{ book_old.cp }}</td>
            </tr>
            <tr>
                <th>Origin</th>
                <td>{{ book_old.origin }}</td>
            </tr>
            <tr>
                <th>RoomName</th>
                <td>{{ book_old.roomName }}</td>
            </tr>
            <tr>
                <th>BookingDate</th>
                <td>{{ book_old.bookingDate ? book_old.bookingDate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>CheckinDate</th>
                <td>{{ book_old.checkinDate ? book_old.checkinDate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>BookingPreviously</th>
                <td>{{ book_old.bookingPreviously }}</td>
            </tr>
            <tr>
                <th>NightNumber</th>
                <td>{{ book_old.nightNumber }}</td>
            </tr>
            <tr>
                <th>TravelReason</th>
                <td>{{ book_old.travelReason }}</td>
            </tr>
            <tr>
                <th>Notes</th>
                <td>{{ book_old.notes }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path(prefix ~ 'index') }}">back to list</a>

    <a href="{{ path(prefix ~ 'edit', {'id': book_old.id}) }}">edit</a>

    {{ include('book_old/_delete_form.html.twig') }}
{% endblock %}
